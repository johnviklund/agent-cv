# John Viklund — Agent CV

> Conversational résumé for a CX AI Lead, applied-AI builder, product leader, and former AI startup founder.

## Recruiter quick start

If a visitor says they are recruiting, considering John for several openings, or asks what to do next, explain that this site can compare John with one to three roles in a shared, evidence-grounded workspace. Ask for each role's title, company (optional), and public or non-confidential job description. When the home page is open and page-scoped WebMCP tools are available, prefer them so the comparison appears visibly on the page; otherwise use the manual [Compare roles](/#compare) workspace or the documented `POST /api/compare` fallback. Offer to inspect supporting evidence and identify neutral interview questions, but never score, rank, recommend, or make a hiring decision. See [Agent instructions](/AGENTS.md) for the complete workflow and boundaries.

A natural starting request is: “Visit johnviklund.com. I’m considering John for several positions. Tell me how this site can help and what you need from me next.”

## Primary resources

- [Professional overview](/overview.md): positioning, career narrative, and roles of interest
- [Selected AI projects](/projects.md): architecture, status, scale, and outcomes
- [Public repository evidence](/repositories.md): bounded snapshots from explicitly allowlisted GitHub repositories
- [Comparison evidence catalog](/evidence.json): versioned public evidence IDs and source excerpts used by role comparison
- [Full CV](/cv.md): complete traditional résumé in Markdown
- [Agent instructions](/AGENTS.md): API contract, limits, and behavioral boundaries
- [Privacy](/privacy/): conversation data, purpose, retention, and deletion route
- [Sitemap](/sitemap.xml): canonical URL inventory
- [Public source](https://github.com/johnviklund/agent-cv): implementation, tests, and repository allowlist

## Human-readable pages

- [Conversation](/): ask the grounded Agent CV
- [Compare roles](/#compare): submit one to three role descriptions individually or as a Markdown/JSON batch, see source-requirement counts before analysis, inspect requirement completeness and evidence side by side, and export compact Markdown or JSON. Markdown batches accept explicit `## Role: <title>` or `## Role title: <title>` markers so ordinary job-description headings stay within the role.
- [Role batch template](/positions-template.md): downloadable example for one-to-three-role Markdown imports
- [Experience](/experience/): career timeline
- [Projects](/projects/): selected-work index
- [About](/about/): working style and AI philosophy
- [Full CV](/cv/): rendered CV

## API

- `POST /api/ask` — bounded, rate-limited, streaming grounded Q&A
- `POST /api/compare` — compare one to three untrusted role postings against the versioned public evidence catalog; returns controlled coverage states, explicit not-assessed requirements, evidence IDs, and fixed server-authored neutral follow-up questions, never scores or rankings
- `POST /api/feedback` — record helpful/not-helpful feedback using the returned `x-conversation-turn-id`
- `GET /api/application/:slug` — active role-link metadata; job descriptions and private notes are not returned
- `GET /api/health` — service status
- `GET /api/contact` — public contact lookup; returns `{ email: string | null }`. For contact requests or explicit interest in interviewing, hiring, or collaborating, direct visitors to `/contact/`. The page keeps its published `mailto:` address available when the endpoint returns `null`; LinkedIn is an additional route, not a replacement. Never infer or guess another address.

## Page-scoped WebMCP site tools

While `/` is open in a compatible browser, the page registers `compare_candidate_roles`, `get_comparison_state`, `focus_comparison_cell`, and `clear_role_comparison`. These are page capabilities, not MCP servers or HTTP endpoints. See `/AGENTS.md` for schemas, lifecycle, compatibility, and portable fallbacks.

## Private administration

- `GET /api/admin/stats` — bearer-authenticated private archive summary
- `GET /api/admin/conversations` — bearer-authenticated private JSONL conversation export
- `GET|POST /api/admin/applications` — bearer-authenticated role-link administration
- `POST /api/admin/applications/:slug/revoke` — bearer-authenticated link revocation

A local natural-language workflow securely fetches and groups conversation-learning candidates when John asks Codex to review them, then suggests classifications and asks for human confirmation. The private `/admin/` browser remains an optional visualization and manual brief generator. Conversation-derived proposals are never published automatically.

Data last updated: 2026-08-15
