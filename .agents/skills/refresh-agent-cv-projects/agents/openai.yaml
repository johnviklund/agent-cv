interface:
  display_name: "Refresh Agent CV Projects"
  short_description: "Review project evidence and update the site"
  default_prompt: "Use $refresh-agent-cv-projects to review repository changes, ask me for decisions, and update my Agent CV site."
policy:
  allow_implicit_invocation: true
