interface:
  display_name: "Review Agent CV Conversations"
  short_description: "Turn conversation gaps into reviewed CV proposals"
  default_prompt: "Use $review-agent-cv-conversations to review my recent Agent CV conversations, identify learning candidates, and interview me about the gaps."
policy:
  allow_implicit_invocation: true
